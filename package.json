{
  "name": "mock-rest-api",
  "version": "1.0.0",
  "description": "",
 "scripts": {
  "tern-on-api": "copy mockApi\\db_back_up.yaml mockApi\\db_stage.yaml && npx yaml-server --hotReload=off --autoStart=off --port 3000 --database ./mockApi/db_stage.yaml",
  "test": "newman run store.collection.json -e store.environment.json",
  "test:html": "newman run store.collection.json -e store.environment.json -r cli,htmlextra --reporter-htmlextra-export newman-report.html"
},
  "keywords": [
    "api",
    "mock",
    "yaml-server",
    "rest"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yaml-server": "^1.10.0"
  },
  "devDependencies": {
    "newman": "^6.2.2",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
